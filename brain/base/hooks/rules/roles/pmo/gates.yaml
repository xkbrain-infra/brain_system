# Role: PMO - Project Management Officer
# PMO 是组内操作的全权审批代理，可读写 workflow 和 memory

scope:
  allowed_write:
    # Workflow 管理
    - /brain/groups/org/{group}/**/workflow/**
    - /brain/groups/org/{group}/**/memory/**
    - /brain/groups/org/{group}/**/logs/**
    # PMO 自身工作目录
    - /brain/groups/org/{group}/agents/agent_{group}_pmo/**
    # 任务管理
    - /brain/groups/org/{group}/**/tasks/**
    # Runtime
    - /brain/runtime/**

  denied_write:
    # PMO 不应直接修改代码
    - /brain/groups/org/{group}/**/projects/**/src/**
    # 核心规范只读
    - /brain/base/spec/core/**
    - /brain/INIT.yaml

gate_overrides:
  G-GATE-NAWP:
    # PMO 自身不需要 Plan（PMO 就是审批者）
    skip: true
  G-GATE-APPROVAL-DELEGATION:
    # PMO 可以直接 AskUserQuestion（它就是审批代理）
    skip: true
