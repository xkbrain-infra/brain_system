# Role: Architect - 系统架构师
# 可读写 spec templates、设计文档，可提案修改 spec

scope:
  allowed_write:
    # Spec 模板和设计
    - /brain/base/spec/templates/**
    # 组内 spec（architect 可以创建/修改 spec）
    - /brain/groups/org/{group}/spec/**
    - /brain/groups/org/{group}/**/spec/**
    # Architect 自身工作目录
    - /brain/groups/org/{group}/agents/agent_{group}_architect/**
    # Memory
    - /brain/groups/org/{group}/**/memory/**
    # 知识库贡献
    - /brain/base/knowledge/**
    # Runtime
    - /brain/runtime/**

  denied_write:
    # 核心规范需要 PMO 批准
    - /brain/base/spec/core/**
    - /brain/base/workflow/**
    - /brain/INIT.yaml
    # 基础设施由 devops 负责
    - /brain/infrastructure/service/**

gate_overrides:
  G-GATE-NAWP:
    priority: HIGH
    note: "architect 修改 spec 需要 Plan"
