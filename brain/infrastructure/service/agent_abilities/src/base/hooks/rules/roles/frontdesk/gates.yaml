# Role: Frontdesk - 前台接待
# 主要处理用户交互，写入权限最小

scope:
  allowed_write:
    # Frontdesk 自身工作目录
    - /brain/groups/org/{group}/agents/agent_{group}_frontdesk/**
    # Memory
    - /brain/groups/org/{group}/**/memory/**
    # Runtime（日志）
    - /brain/runtime/**

  denied_write:
    # 核心规范只读
    - /brain/base/**
    - /brain/INIT.yaml
    # 基础设施
    - /brain/infrastructure/**
    # 项目代码
    - /brain/groups/org/{group}/**/projects/**
    # Spec
    - /brain/groups/org/{group}/spec/**

gate_overrides:
  G-GATE-APPROVAL-DELEGATION:
    # Frontdesk 可以直接 AskUserQuestion（它是用户接口）
    skip: true
