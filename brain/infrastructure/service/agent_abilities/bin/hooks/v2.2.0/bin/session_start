#!/usr/bin/env python3
"""session_start Hook - v2 入口 (自动生成)"""
import sys
import os

HOOK_ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(HOOK_ROOT, "src", "handlers", "session", "current", "python"))

from handler import handle_session_start

if __name__ == "__main__":
    handle_session_start()
