#!/bin/bash
# Performance Test for System Status - Entry point

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
MONITOR_ROOT="$(dirname "$SCRIPT_DIR")"
SRC_DIR="$MONITOR_ROOT/src"

exec python3 "$SRC_DIR/perf_test_system_status.py" "$@"
