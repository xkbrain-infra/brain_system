#!/usr/bin/env python3
"""session_start Hook - v3.0.0 入口"""
import sys
import os

HOOK_ROOT = "/brain/infrastructure/service/agent_abilities"
sys.path.insert(0, os.path.join(HOOK_ROOT, "src", "hooks", "handlers", "session", "current", "python"))

from handler import handle_session_start

if __name__ == "__main__":
    handle_session_start()
