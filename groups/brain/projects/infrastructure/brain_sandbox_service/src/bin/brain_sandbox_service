#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
SERVICE_ROOT=$(cd "${SCRIPT_DIR}/.." && pwd)

exec python3 "${SERVICE_ROOT}/current/sandbox_service.py" "$@"
