#!/usr/bin/env python3
"""Timer service entry point."""
import sys
import os

# Add src to path (parent of timer package)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))

from timer.service_timer import main

if __name__ == "__main__":
    main()
