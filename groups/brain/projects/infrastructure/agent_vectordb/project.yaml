name: agent_vectordb
description: MCP VectorDB 服务
version: 1.0.0
type: service
language: python

paths:
  project_root: /xkagent_infra/groups/brain/projects/infrastructure/agent_vectordb
  infra_service: /xkagent_infra/brain/infrastructure/service/agent_vectordb

dependencies: []

build:
  type: python
  entry_point: mcp_server.py

deploy:
  method: copy
  config_files:
    - requirements.txt
